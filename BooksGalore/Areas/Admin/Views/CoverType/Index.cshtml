@model IEnumerable<CoverType>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["title"] = "List of Categories";
}
@*<partial name="_toastr.cshtml" />
*@<div class="container" width="70%" style="margin:50px auto">
    <div class=row pt-3>
    <div class="mb-3 col-6">
        <h2 class="text-info">Categories</h2>
    </div>
    <div class="mb-3 col-6 text-end" >
        <a  asp-controller="CoverType" asp-action="Create" class=" btn btn-outline-success"><i class="bi-plus-square-fill"></i>&nbsp New CoverType</a>
        </div>
    </div>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>
                    CoverType
                </th>
            
                <th>

                </th>
             
            </tr>
        </thead>
        <tbody>
          
            @foreach (var i in Model)
            {
                <tr>
                    <td>
                        @i.Name
                    </td>
           
                    <td class="text-center">
                        <div class="btn-group">
                            <a  class="btn btn-outline-info" asp-controller="CoverType" asp-action="Edit" asp-route-id=@i.Id>Edit</a>
                            <a  class="btn btn-outline-danger" asp-controller="CoverType" asp-action="Delete" asp-route-id=@i.Id>Delete</a>

                        </div>
                    </td>
                </tr>
            }

        </tbody>
    </table>


</div>

@section myown
{
    <partial name="_toastr.cshtml"/>
}
